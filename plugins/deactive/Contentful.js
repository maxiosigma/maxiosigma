//const contentful = require('contentful')
//const contentful = require('contentful-management')

//const config = {
//	space: process.env.CTF_SPACE_ID,
//	accessToken: process.env.CTF_CDA_ACCESS_TOKEN,
//}

//const client = contentful.createClient({ accessToken: config.accessToken })

//const client =

//export default ({ $config: { sps, atk } }) => {
//	return contentful.createClient({ space: sps, accessToken: atk })
//}

//export const createClient = ({ config: { sps, atk } }) => {
//	console.log(sps, atk)
//	return contentful.createClient({ space: sps, accessToken: atk })
//}

//module.exports = {
//	createClient(config) {
//		return contentful.createClient({ space: config.sps, accessToken: config.atk })
//	},
//	//createClient() {
//	//	return client.getSpace(config.space)
//	//},
//}

// contentful.createClient(config).sync()
