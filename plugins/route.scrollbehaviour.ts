export default defineNuxtPlugin(async (nuxtApp) => {
    //nuxtApp.vueApp  .$router.options.scrollBehavior = (to, from, savedPosition) => {
    //	if (savedPosition) {
    //	  return savedPosition
    //	} else {
    //	  return { top: 0 }
    //	}
    //  }
})
