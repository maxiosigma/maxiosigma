//import data from "~/assets/gql/works.graphql";

export default async function () {
	//const graphql = useStrapiGraphQL();
	//const gql = await graphql(data);
	////<svg-icon name="sample" />
	////<svg-icon name="my-sprite/my-image" />
	//console.log(
	//	gql.data.works.data.map(
	//		(it) => it
	//		//it.attributes.map((jt) => {
	//		//	return { ...jt, media: jt.media.data };
	//		//})
	//	)
	//);
	//return works;
	//useState("works", () => works)
	//return useState("foo", () => "bar");
}
