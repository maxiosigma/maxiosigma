//const getFile = () => import('~/data.json').then((m) => m.default || m)

export const actions = {
	async getDataFromFile({ commit }) {
		//console.log(getFile)
		//let itemsCategories = [],
		//	newsCategories = []
		//try {
		//	const { categories, otherCategories } = await getFile()
		//	if (categories) itemsCategories = categories
		//	if (otherCategories) newsCategories = otherCategories
		//} catch (e) {
		//	console.error({ e })
		//}
		//commit('items/SET_ITEMS_CATEGORIES', itemsCategories, {
		//	root: true,
		//})
		//commit('news/SET_NEWS_CATEGORIES', newsCategories, {
		//	root: true,
		//})
	},
}
